parallel: true
ruby_version: 3.3
ignore:
  # Auto-generated by racc (grammar.y → grammar.y.rb) and oedipus_lex
  # (lexer.rex → lexer.rex.rb). Regenerated by `rake generate`, so lint
  # warnings here would only be reintroduced on every rebuild.
  - "api_query_language/lib/api_query_language/**/expression_parser.y.rb"
  - "api_query_language/lib/api_query_language/**/expression_lexer.rex.rb"
